services:
  qwen-engine:
    build: .
    ports:
      - "8090:8000"
    volumes:
      - ./data:/app/data
      - ./server:/app/server
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
    restart: unless-stopped
